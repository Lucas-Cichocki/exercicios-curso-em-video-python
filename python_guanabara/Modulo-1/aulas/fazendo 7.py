import random

N = random.randint(1, 10) #ou use random.random, porem assim ele ira gerar um numero quebrado entre 0 e 1, radint é apenas random porem com int no final, para lembrar a escrita

print(N)